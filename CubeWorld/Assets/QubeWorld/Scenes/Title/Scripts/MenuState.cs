namespace Title
{
    public enum MenuState
    {
        NONE, // 未表示状態
        TITLE,
        GENERATOR,
        OPTIONS,
        PAUSE,
        JOIN_MULTIPLAYER,
        LOAD,
        ABOUT
    }
}
